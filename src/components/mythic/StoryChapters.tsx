import type { Legend } from '../../types/legend';
export function StoryChapters({ legend }: { legend: Legend }) { return <section className="chapters" id="story"><div className="section-head"><p className="eyebrow">Story Engine</p><h2>{legend.hero.subtitle}</h2></div>{legend.chapters.map((c, i) => <article key={c.id} className={`chapter tone-${c.tone}`}><div className="chapter-index">{String(i+1).padStart(2,'0')}</div><div><p className="eyebrow">{c.period}</p><h3>{c.title}</h3><p>{c.summary}</p><ul>{c.beats.map((b) => <li key={b}>{b}</li>)}</ul></div></article>)}</section>; }
